"""
Конфигурация проекта для автоматической обработки и модерации постов из Telegram-каналов.

ВАЖНО: Все чувствительные данные (токены, ключи) должны храниться в .env файле,
который НЕ коммитится в Git. В этом файле указаны только значения по умолчанию.

Структура .env файла:
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
OPENAI_API_KEY=ваш_openai_ключ
OPENAI_PROXY=socks5://user:pass@host:port  # Опционально
"""

import os
from pathlib import Path

# Загружаем переменные из .env файла, если он существует
_env_file = Path(__file__).parent / ".env"
if _env_file.exists():
    with open(_env_file, "r", encoding="utf-8") as f:
        for line in f:
            line = line.strip()
            if line and not line.startswith("#") and "=" in line:
                key, value = line.split("=", 1)
                # Убираем кавычки если есть
                value = value.strip('"\'')
                # Устанавливаем переменную окружения только если она еще не установлена
                if key not in os.environ:
                    os.environ[key] = value

# ==========================
# TELEGRAM БОТ (модерационный)
# ==========================
# Токен бота для модерации (получить у @BotFather)
# ВАЖНО: Не коммитьте реальный токен в Git! Используйте переменную окружения
TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN", "8240444233:AAFAP4ItzBvko53z07QoV8YvkfxRDe5UuQA")

# ID операторов (модераторов) - список user_id из Telegram
# Узнать свой user_id можно через @userinfobot или @getidsbot
MODERATOR_IDS = [
    1522224786,  # Оператор
]

# ==========================
# TELEGRAM ЮЗЕР-БОТ (Telethon)
# ==========================
# API ID и API Hash для Telethon
# Получить на https://my.telegram.org/apps
# ВАЖНО: Не коммитьте реальные ключи в Git! Используйте переменные окружения
TELEGRAM_API_ID = int(os.getenv("TELEGRAM_API_ID", "0")) or 32446088
TELEGRAM_API_HASH = os.getenv("TELEGRAM_API_HASH", "626b7d4e2ba80b62c1904938a845613d")

# Номер телефона для юзер-бота (в международном формате, например: +79991234567)
TELEGRAM_PHONE = "+79122812193"

# Сессия Telethon будет сохранена в файл (автоматически при первом входе)
TELEGRAM_SESSION_FILE = "telethon_session.session"

# ==========================
# ИСХОДНЫЕ КАНАЛЫ (откуда читаем посты)
# ==========================
# Список ID или username исходных каналов
# Можно указать как ID (например: -1001234567890) или username (например: "channelname")
SOURCE_CHANNEL_IDS = [
    "@elitetennis",  # Канал 1
    "@primetennis",  # Канал 2
]

# ==========================
# ЦЕЛЕВЫЕ КАНАЛЫ (куда публикуем)
# ==========================
# Список ID или username целевых каналов
# Можно указать как ID (например: -1001234567890) или username (например: "targetchannel")
TARGET_CHANNEL_IDS = [
    "@S360streams",  # Username группы (предпочтительно)
    # -4999682913,  # ID группы (с минусом для групп) - если username не работает
]

# ==========================
# OPENAI GPT
# ==========================
# API ключ OpenAI (получить на https://platform.openai.com/api-keys)
# ВАЖНО: Не коммитьте реальный ключ в Git! Используйте переменную окружения или .env файл
OPENAI_API_KEY = os.getenv("OPENAI_API_KEY", "sk-your-api-key-here")

# Прокси для OpenAI API (если нужен, формат: socks5://user:pass@host:port или http://user:pass@host:port)
# Для SOCKS5 используй формат: socks5://user:pass@host:port
# Оставь пустым если прокси не нужен
OPENAI_PROXY = os.getenv("OPENAI_PROXY", "")

# Модель GPT
OPENAI_MODEL = "gpt-4o"

# Промпт для GPT
GPT_PROMPT = """Ты — редактор и верстальщик новостей по теннису для бренда Setka360.

Твой результат — готовый пост для Telegram-канала в формате HTML (parse_mode=HTML).

ВХОД:
Я даю тебе сырой текст новости по теннису (иногда с лишними деталями, без эмодзи и без структуры).

ВЫХОД:
Ты возвращаешь JSON с двумя полями:
1. "html_text" — готовый оформленный текст поста в HTML (без пояснений, комментариев)
2. "image_query" — короткий запрос для поиска картинки на английском (2-5 слов, например: "tennis match WTA indoor")

ОБЩИЙ СТИЛЬ:
- Пиши живым, понятным языком, без канцелярита.
- Длина поста: 1 яркий заголовок + 2–5 абзацев основного текста.
- Используй эмодзи, но не злоупотребляй: 1–3 эмодзи на абзац максимум.
- Весь текст и эмодзи — на русском, кроме имён турниров/игроков/городов.

РАЗМЕТКА ДЛЯ TELEGRAM (HTML):
- Используй ТОЛЬКО HTML-теги: <b>, <i>, <u>, <s>, <a>.
- Не используй Markdown, <code>, <pre> и "красивые" псевдо-шрифты из Unicode.
- Заголовок делай жирным: на первой строке вида:
  🎾 <b>Короткий заголовок до 70 символов</b>
- Внутри текста:
  - важные факты/фразы можешь выделять <b>жирным</b>;
  - названия турниров, туров, стадий можно делать <i>курсивом</i>.

ЭМОДЗИ И СМЫСЛОВОЕ ОФОРМЛЕНИЕ:
- Всегда держи общий теннисный вайб: 🎾, 🟢, ⭐, 🏆, ⏱, 📊, 🔥, ❄️ и т.п.
- Используй эмодзи по смыслу:
  - про уверенную победу: 🏆🔥
  - про тяжёлый матч: 💥🔥, ⏱
  - про рекорды и рейтинги: 📊⭐
  - про турниры и финалы: 🏆🎾
- Не ставь эмодзи в каждое предложение. Лучше в начале/конце абзаца или в заголовке.

ИГРОКИ И ФЛАГИ:
- Определяй имена теннисистов в тексте (имя + фамилия).
- Если тебе известна страна игрока — ставь флаг ПЕРЕД именем при первом упоминании:
  🇷🇺 Даниил Медведев, 🇪🇸 Карлос Алькарас, 🇵🇱 Ига Швёнтек и т.п.
- Если ты НЕ уверен в стране игрока — флаг НЕ ставь.
- Если в тексте явно указана страна (например, «российская теннисистка Дарья Касаткина») — используй соответствующий флаг при первом упоминании.
- Дальше по тексту можно писать имя без флага.

СЧЁТЫ, ТУРНИРЫ, ФАКТЫ:
- Счёт матчей выделяй жирным и пиши компактно:
  <b>6:4, 3:6, 7:6(5)</b>
- Названия турниров и категорий выделяй курсивом:
  <i>WTA 500 в Берлине</i>, <i>ATP 250</i>, <i>Челленджер</i>.
- Если у новости есть 2–4 ключевых факта — вынеси их в маркированный список в конце:
  🔹 факт 1
  🔹 факт 2
  🔹 факт 3
  (используй либо 🔹, либо •, но единообразно)

ХЕШТЕГИ:
- В самом конце, с новой строки, сделай строку хештегов:
  #теннис #Setka360 #ATP #WTA
- Не больше 5–7 хештегов, только по сути.

ФИНАЛЬНЫЕ ТРЕБОВАНИЯ:
- Возвращай ТОЛЬКО JSON в формате:
  { "html_text": "готовый HTML-текст поста", "image_query": "tennis match WTA" }
- Не добавляй фразу, что текст написан ИИ.
- Если исходный текст очень сырой — аккуратно дополни контекст, но не выдумывай факты (счёт, имена, результаты не придумывай)."""

# Максимальное количество попыток при ошибках GPT
GPT_MAX_RETRIES = 3

# Задержка между попытками (секунды) - экспоненциальная: 1, 2, 4
GPT_RETRY_DELAYS = [1, 2, 4]

# ==========================
# PEXELS API (для поиска картинок)
# ==========================
# API ключ Pexels (получить на https://www.pexels.com/api/)
# ВАЖНО: Не коммитьте реальный ключ в Git! Используйте переменную окружения или .env файл
PEXELS_API_KEY = os.getenv("PEXELS_API_KEY", "2sEQzxTl8QPLIJ3YFCZpNwDtGe4KqRvys7fg5CQKBXosSmkYcS4F0EXN")
PEXELS_API_URL = "https://api.pexels.com/v1/search"
PEXELS_PER_PAGE = 3  # Количество картинок для выбора

# ==========================
# СЕРВИС СТИЛИЗАЦИИ ИЗОБРАЖЕНИЙ
# ==========================
# URL микросервиса для стилизации изображений
IMAGE_RENDER_SERVICE_URL = os.getenv("IMAGE_RENDER_SERVICE_URL", "http://localhost:8000")
# Путь для сохранения стилизованных изображений на сервере
RENDERED_IMAGES_DIR = Path(__file__).parent / "rendered_images"

# ==========================
# БАЗА ДАННЫХ
# ==========================
# Путь к файлу SQLite БД
DATABASE_PATH = "posts.db"

# ==========================
# ЛОГИРОВАНИЕ
# ==========================
# Уровень логирования: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL = "INFO"

# Путь к директории с логами
LOG_DIR = "logs"

# Имя файла лога
LOG_FILE = "moderation_bot.log"

# Максимальный размер лог-файла (в байтах) перед ротацией
LOG_MAX_BYTES = 10 * 1024 * 1024  # 10 MB

# Количество резервных лог-файлов
LOG_BACKUP_COUNT = 5

# ==========================
# ПАРСИНГ ХЭШТЕГОВ
# ==========================
# Максимальная длина оригинального текста для показа оператору (символов)
ORIGINAL_TEXT_PREVIEW_LENGTH = 400

