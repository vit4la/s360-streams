# Система автоматической модерации постов из Telegram-каналов

Система для автоматической обработки постов из исходных Telegram-каналов через GPT и модерации операторами перед публикацией в целевые каналы.

## Возможности

- 📥 **Автоматический сбор постов** из исходных Telegram-каналов через Telethon (юзер-бот)
- 🤖 **Обработка через GPT-4o** - переписывание текста, создание заголовков и хэштегов
- 👥 **Модерация через Telegram-бота** - операторы получают черновики и могут их одобрить, отредактировать или отклонить
- 📢 **Публикация в несколько каналов** - возможность выбрать один или несколько целевых каналов
- 🖼️ **Поддержка картинок** - оператор может прикрепить картинку при публикации
- 📊 **База данных SQLite** - хранение всех постов и черновиков
- 📝 **Подробное логирование** с ротацией файлов

## Архитектура

Система состоит из трёх основных компонентов:

1. **Telethon Listener** - слушает новые сообщения из исходных каналов и сохраняет их в БД
2. **GPT Worker** - обрабатывает новые посты через GPT и создаёт черновики для модерации
3. **Moderation Bot** - бот для модерации, через который операторы управляют черновиками

Все компоненты работают параллельно и общаются через общую базу данных SQLite.

## Установка

### 1. Клонирование и установка зависимостей

```bash
cd translations
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Откройте файл `config_moderation.py` и заполните все необходимые параметры:

#### Telegram Bot (модерационный бот)
- `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- `MODERATOR_IDS` - список ID операторов (узнать можно через @userinfobot)

#### Telethon (юзер-бот для чтения исходных каналов)
- `TELEGRAM_API_ID` - получить на https://my.telegram.org/apps
- `TELEGRAM_API_HASH` - получить на https://my.telegram.org/apps
- `TELEGRAM_PHONE` - номер телефона в международном формате (например: +79991234567)

#### Каналы
- `SOURCE_CHANNEL_IDS` - список исходных каналов (откуда читаем посты)
  - Можно указать как username: `["@channel1", "@channel2"]`
  - Или как ID: `["-1001234567890", "-1001234567891"]`
- `TARGET_CHANNEL_IDS` - список целевых каналов (куда публикуем)
  - Аналогично, можно указать username или ID

#### OpenAI GPT
- `OPENAI_API_KEY` - API ключ от OpenAI (получить на https://platform.openai.com/api-keys)
- `OPENAI_MODEL` - модель GPT (по умолчанию "gpt-4o")

### 3. Первый запуск Telethon

При первом запуске Telethon попросит ввести код подтверждения из Telegram. Это нужно сделать один раз, после чего сессия сохранится в файл `telethon_session.session`.

**Важно:** Убедитесь, что юзер-аккаунт (номер телефона) подписан на все исходные каналы!

## Запуск

### Запуск всей системы модерации

```bash
python moderation_main.py
```

**Важно:** Этот скрипт работает параллельно с существующим `vk_to_telegram.py` и не конфликтует с ним.

Система запустит все три компонента:
- Telethon слушатель начнёт читать посты из исходных каналов
- GPT воркер начнёт обрабатывать новые посты
- Бот модерации начнёт отправлять черновики операторам

### Запуск отдельных компонентов (для тестирования)

```bash
# Только Telethon слушатель
python telethon_listener.py

# Только GPT воркер
python gpt_worker.py

# Только бот модерации
python moderation_bot.py
```

## Работа с системой

### Для операторов

1. **Начало работы**: Отправьте боту команду `/start`
2. **Получение черновиков**: Бот автоматически отправляет новые черновики на модерацию
3. **Действия с черновиком**:
   - **✅ Опубликовать** - выбрать целевой канал(ы) и опубликовать пост
   - **✏️ Править** - отредактировать текст, заголовок и хэштеги
   - **🚫 Отклонить** - отклонить черновик

### Процесс публикации

1. Оператор нажимает "✅ Опубликовать"
2. Выбирает один или несколько целевых каналов
3. Может прикрепить картинку (или нажать "Без картинки")
4. Пост публикуется во все выбранные каналы

### Редактирование текста

1. Оператор нажимает "✏️ Править"
2. Отправляет новый текст одним сообщением:
   - Первая строка - заголовок
   - Остальные строки - текст поста
   - Хэштеги можно указать в любом месте (начинаются с #)
3. Бот показывает превью с обновлённым текстом
4. Можно редактировать несколько раз подряд

## Структура базы данных

### Таблица `source_posts`
- `id` - ID поста
- `channel_id` - ID канала
- `message_id` - ID сообщения
- `text_original` - Оригинальный текст поста
- `date` - Дата поста
- `status` - Статус: `new`, `processed`

### Таблица `draft_posts`
- `id` - ID черновика
- `source_post_id` - Ссылка на исходный пост
- `title` - Заголовок (от GPT или оператора)
- `body` - Текст поста
- `hashtags` - Хэштеги (строка)
- `gpt_response_raw` - Сырой JSON ответ GPT
- `status` - Статус: `pending_moderation`, `published`, `rejected`
- `target_chat_id` - ID целевого канала (после публикации)
- `target_message_id` - ID сообщения в целевом канале

## Логирование

Логи сохраняются в директории `logs/` с ротацией:
- Основной файл: `logs/moderation_bot.log`
- При достижении 10 MB создаётся новый файл
- Хранится до 5 резервных копий

Уровень логирования настраивается в `config.py` (параметр `LOG_LEVEL`).

## Безопасность

⚠️ **Важно:**
- Не коммитьте файл `config.py` с реальными токенами в Git
- Файл `config.py` уже добавлен в `.gitignore`
- Храните токены и API ключи в безопасном месте
- Не делитесь файлом `telethon_session.session` - это ваша сессия Telegram

## Устранение неполадок

### Telethon не подключается
- Проверьте правильность `TELEGRAM_API_ID` и `TELEGRAM_API_HASH`
- Убедитесь, что номер телефона указан в международном формате
- При первом запуске введите код подтверждения из Telegram

### Бот не отправляет черновики
- Проверьте, что `MODERATOR_IDS` содержит правильные ID операторов
- Убедитесь, что оператор отправил боту `/start`
- Проверьте логи на наличие ошибок

### GPT не обрабатывает посты
- Проверьте правильность `OPENAI_API_KEY`
- Убедитесь, что на счёте OpenAI есть средства
- Проверьте логи на ошибки API

### Ошибки при публикации в каналы
- Убедитесь, что бот добавлен в целевые каналы как администратор
- Проверьте правильность ID каналов в `TARGET_CHANNEL_IDS`
- Проверьте права бота на отправку сообщений

## Лицензия

Проект создан для внутреннего использования.
