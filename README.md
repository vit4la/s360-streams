## Автопостинг трансляций: VK → Telegram

Этот небольшой скрипт берёт посты с трансляциями тенниса из группы VK и отправляет их в Telegram‑группу как **альбом (медиагруппу)**: несколько превью‑картинок + общий текст поста.

### Что именно делает скрипт

- **Каждые 15 минут** (настраивается в cron) опрашивает стену группы `tennisprimesport` (или другую, которую укажете).
- Берёт только посты:
  - у которых есть вложения `video`;
  - и в тексте есть что‑то из:
    - `WTA`, `ATP`;
    - `Ролан Гаррос` / `Roland Garros`;
    - `Открытый чемпионат Австралии` / `Australian Open`;
    - `Открытый чемпионат США` / `US Open`;
    - `Уимблдон` / `Wimbledon`;
    - **или** в тексте есть минимум два флага‑эмодзи и знак `-` между ними (формат `🇫🇷 Имя Фамилия - 🇷🇺 Имя Фамилия`).
- Для каждого такого поста:
  - вытаскивает превью картинок из всех видео‑вложений;
  - отправляет их в ТГ как **альбом** с одной общей подписью;
  - подпись — это **оригинальный текст поста из VK без изменений**;
  - запоминает `post_id`, чтобы не постить одно и то же дважды.
- При первом запуске просто **запоминает текущий максимум `post_id` и ничего не отправляет**, чтобы в ТГ не поехала вся история.

---

## Подготовка токенов

### 1. VK: приложение и токен

1. Зайди на VK: `vk.com` → раздел **Для разработчиков** → создай приложение типа **Standalone**.
2. Получи `access_token` с правами: `wall`, `groups`.
3. Найди **ID группы**:
   - зайди в нужную группу, например `[https://vk.com/tennisprimesport](https://vk.com/tennisprimesport)`;
   - в dev‑tools VK или через метод `groups.getById` посмотри `id` — это и есть `VK_GROUP_ID` **без минуса**.

### 2. Telegram: бот и chat_id

1. В Telegram через `@BotFather` создай бота и получи `TELEGRAM_BOT_TOKEN`.
2. Добавь бота в свою **Telegram‑группу** и дай ему права на отправку сообщений.
3. Узнай `TELEGRAM_CHAT_ID`:
   - самый простой способ — временно написать маленький скрипт/бота, который печатает `chat_id` при любом сообщении в группе, либо использовать готового бота‑информер.

---

## Настройка проекта на VPS (Linux)

Предполагается, что на сервере есть Python 3.8+.

```bash
cd /path/to/project   # положи сюда файлы vk_to_telegram.py и requirements.txt

python3 -m venv venv
source venv/bin/activate

pip install --upgrade pip
pip install -r requirements.txt
```

Далее открой файл `vk_to_telegram.py` и в блоке **CONFIG** вверху заполни:

- `VK_TOKEN`
- `VK_GROUP_ID` (без минуса)
- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHAT_ID`

Проверь, что всё работает:

```bash
source venv/bin/activate
python vk_to_telegram.py
```

При первом запуске в логе увидишь сообщение вида:

> Первый запуск: инициализировали last_post_id=..., отправка постов не выполнялась.

Это нормально. Со следующего запуска начнут уходить новые посты с трансляциями.

---

## Запуск по расписанию (каждые 15 минут)

Открой cron:

```bash
crontab -e
```

И добавь строку (путь и пользователь подставь свои):

```bash
*/15 * * * * cd /path/to/project && /path/to/project/venv/bin/python vk_to_telegram.py >> vk_to_telegram.log 2>&1
```

После этого каждые 15 минут будет:

- опрашиваться стена VK;
- отбираться только посты с трансляциями по заданным правилам;
- отправляться в твою Telegram‑группу как альбомы с оригинальным текстом.


