# Система автоматической модерации новостей из Telegram-каналов

## Общее описание

Система автоматически читает посты из указанных Telegram-каналов, обрабатывает их через GPT-4o для переписывания, и отправляет черновики операторам для модерации. После одобрения оператором посты публикуются в целевые каналы.

## Архитектура системы

Система состоит из 4 основных компонентов:

1. **TelethonListener** - читает новые посты из исходных каналов через Telethon (юзер-бот)
2. **GPTWorker** - обрабатывает новые посты через OpenAI GPT-4o и создаёт черновики
3. **ModerationBot** - Telegram-бот для операторов (модерация, редактирование, публикация)
4. **Database** - SQLite база данных для хранения постов и черновиков

## Компоненты системы

### 1. TelethonListener (`telethon_listener.py`)

**Назначение:** Читает новые сообщения из исходных Telegram-каналов через Telethon API.

**Как работает:**
- Подключается к Telegram через Telethon как юзер-бот (используя номер телефона)
- Подписывается на указанные исходные каналы (`SOURCE_CHANNEL_IDS`)
- При получении нового сообщения:
  - Извлекает текст поста
  - Сохраняет `channel_id` и `message_id` для защиты от дублей
  - Сохраняет оригинальный текст в таблицу `source_posts` со статусом `'new'`
  - Пропускает сообщения без текста

**Конфигурация:**
- `TELEGRAM_API_ID` - API ID из https://my.telegram.org/apps
- `TELEGRAM_API_HASH` - API Hash из https://my.telegram.org/apps
- `TELEGRAM_PHONE` - номер телефона в формате +79991234567
- `TELEGRAM_SESSION_FILE` - файл для сохранения сессии (по умолчанию `telethon_session.session`)
- `SOURCE_CHANNEL_IDS` - список исходных каналов (например: `["@elitetennis", "@primetennis"]`)

**База данных:**
- Таблица `source_posts`:
  - `id` - уникальный ID поста
  - `channel_id` - ID или username канала
  - `message_id` - ID сообщения в канале
  - `text_original` - оригинальный текст поста
  - `photo_file_id` - file_id картинки (если есть, пока не используется)
  - `date` - дата поста
  - `status` - статус: `'new'` (новый, ожидает обработки GPT)
  - `created_at` - время создания записи
  - UNIQUE(channel_id, message_id) - защита от дублей

### 2. GPTWorker (`gpt_worker.py`)

**Назначение:** Обрабатывает новые посты через OpenAI GPT-4o и создаёт черновики для модерации.

**Как работает:**
- Каждые 5 секунд проверяет БД на наличие постов со статусом `'new'`
- Для каждого нового поста:
  - Отправляет оригинальный текст в GPT-4o с промптом на переписывание
  - GPT возвращает JSON: `{"title": "...", "body": "...", "hashtags": ["...", "..."]}`
  - Создаёт черновик в таблице `draft_posts` со статусом `'pending_moderation'`
  - Обновляет статус исходного поста на `'processed'`
  - Сохраняет полный ответ GPT в `gpt_response_raw` для отладки

**Обработка ошибок:**
- При ошибке GPT делает до 3 попыток с задержками: 1, 2, 4 секунды
- При неудаче логирует ошибку и пропускает пост (статус остаётся `'new'`)

**Конфигурация:**
- `OPENAI_API_KEY` - API ключ OpenAI
- `OPENAI_MODEL` - модель GPT (по умолчанию `"gpt-4o"`)
- `OPENAI_PROXY` - прокси для OpenAI API (если нужен, формат: `socks5://user:pass@host:port`)
- `GPT_PROMPT` - промпт для GPT
- `GPT_MAX_RETRIES` - максимальное количество попыток (по умолчанию 3)
- `GPT_RETRY_DELAYS` - задержки между попытками (по умолчанию [1, 2, 4] секунды)

**База данных:**
- Таблица `draft_posts`:
  - `id` - уникальный ID черновика
  - `source_post_id` - ссылка на исходный пост
  - `title` - заголовок от GPT
  - `body` - текст от GPT
  - `hashtags` - хэштеги от GPT (строка, разделённая пробелами)
  - `gpt_response_raw` - полный JSON ответ от GPT
  - `status` - статус: `'pending_moderation'` (ожидает модерации), `'approved'` (одобрен), `'rejected'` (отклонён), `'published'` (опубликован)
  - `target_chat_id` - ID канала, куда опубликован
  - `target_message_id` - ID сообщения в целевом канале
  - `created_at` - время создания
  - `updated_at` - время последнего обновления

### 3. ModerationBot (`moderation_bot.py`)

**Назначение:** Telegram-бот для операторов для модерации, редактирования и публикации черновиков.

**Как работает:**

**Автоматическая отправка черновиков:**
- Каждые 10 секунд проверяет БД на новые черновики со статусом `'pending_moderation'`
- Отправляет черновик всем операторам из `MODERATOR_IDS`
- Формат сообщения:
  - Ссылка на исходный пост
  - Оригинальный текст (обрезанный до 400 символов)
  - Вариант от GPT (заголовок, текст, хэштеги)
  - Кнопки: "✅ Опубликовать", "✏️ Править", "🚫 Отклонить"

**Действия оператора:**

1. **Опубликовать:**
   - Если один целевой канал - сразу выбор картинки
   - Если несколько - выбор каналов (можно несколько)
   - Варианты публикации:
     - "🖼️ С исходной картинкой" - получает картинку из исходного поста через `forwardMessage`
     - "📸 Прикрепить свою" - оператор отправляет свою картинку
     - "Без картинки" - публикация без картинки
   - Публикует во все выбранные каналы
   - При ошибке в одном канале продолжает публикацию в другие
   - Обновляет статус черновика на `'published'`

2. **Править:**
   - Оператор отправляет новый текст
   - Система автоматически парсит хэштеги из текста (строки начинающиеся с #)
   - Обновляет черновик в БД
   - Можно редактировать несколько раз

3. **Отклонить:**
   - Обновляет статус черновика на `'rejected'`
   - Черновик больше не показывается операторам

**Конфигурация:**
- `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- `MODERATOR_IDS` - список user_id операторов
- `TARGET_CHANNEL_IDS` - список целевых каналов для публикации
- `ORIGINAL_TEXT_PREVIEW_LENGTH` - длина превью оригинального текста (по умолчанию 400)

### 4. Database (`database.py`)

**Назначение:** Работа с SQLite базой данных.

**Методы:**

**Для исходных постов:**
- `add_source_post()` - добавить новый исходный пост
- `get_new_source_posts()` - получить все посты со статусом `'new'`
- `mark_source_post_processed()` - пометить пост как обработанный

**Для черновиков:**
- `add_draft_post()` - создать новый черновик
- `get_pending_draft_posts()` - получить все черновики со статусом `'pending_moderation'`
- `get_draft_post()` - получить черновик по ID
- `update_draft_post()` - обновить черновик (при редактировании)
- `mark_draft_published()` - пометить черновик как опубликованный
- `mark_draft_rejected()` - пометить черновик как отклонённый

## Главный модуль (`moderation_main.py`)

**Назначение:** Запускает все компоненты системы и управляет их жизненным циклом.

**Как работает:**
1. Инициализирует базу данных
2. Запускает все компоненты параллельно:
   - TelethonListener (в фоне)
   - GPTWorker (в фоне)
   - ModerationBot (в фоне)
3. Работает до получения сигнала остановки (SIGINT, SIGTERM)
4. При остановке корректно завершает все компоненты

**Проверка конфигурации:**
- Проверяет наличие всех обязательных параметров перед запуском
- Выводит ошибку и завершает работу, если что-то не настроено

## Конфигурация (`config_moderation.py`)

Все настройки системы хранятся в `config_moderation.py`. Файл автоматически загружает переменные из `.env` файла.

### Обязательные переменные (должны быть в `.env` или в `config_moderation.py`):

**Telegram бот:**
- `TELEGRAM_BOT_TOKEN` - токен бота модерации

**Telethon (юзер-бот):**
- `TELEGRAM_API_ID` - API ID
- `TELEGRAM_API_HASH` - API Hash
- `TELEGRAM_PHONE` - номер телефона

**OpenAI:**
- `OPENAI_API_KEY` - API ключ OpenAI
- `OPENAI_PROXY` - прокси (опционально, если нужен)

### Настройки в `config_moderation.py`:

```python
# Операторы
MODERATOR_IDS = [1522224786]  # Список user_id операторов

# Исходные каналы (откуда читаем)
SOURCE_CHANNEL_IDS = ["@elitetennis", "@primetennis"]

# Целевые каналы (куда публикуем)
TARGET_CHANNEL_IDS = ["@S360streams"]

# База данных
DATABASE_PATH = "posts.db"

# Логирование
LOG_DIR = "logs"
LOG_FILE = "moderation_bot.log"
LOG_LEVEL = "INFO"
```

## Установка и запуск

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Зависимости:
- `python-telegram-bot` - для бота модерации
- `telethon` - для чтения постов из каналов
- `openai` - для работы с GPT
- `httpx` - для HTTP запросов (используется OpenAI SDK)
- `python-dotenv` - для загрузки .env файла

### 2. Настройка `.env` файла

Создайте файл `.env` в корне проекта:

```bash
# Telegram бот модерации
TELEGRAM_BOT_TOKEN=8240444233:AAFAP4ItzBvko53z07QoV8YvkfxRDe5UuQA

# Telethon (юзер-бот)
TELEGRAM_API_ID=32446088
TELEGRAM_API_HASH=626b7d4e2ba80b62c1904938a845613d

# OpenAI
OPENAI_API_KEY=sk-proj-...
OPENAI_PROXY=socks5://user:pass@host:port  # Опционально
```

### 3. Первый запуск Telethon

При первом запуске Telethon попросит ввести код подтверждения из Telegram:

```bash
python3 moderation_main.py
```

Введите код, который придёт в Telegram на указанный номер телефона.

### 4. Запуск как systemd сервис

**Создание сервиса:**

```bash
# Скопируйте файл сервиса
cp moderation-bot.service /etc/systemd/system/

# Перезагрузите systemd
systemctl daemon-reload

# Включите автозапуск
systemctl enable moderation-bot.service

# Запустите сервис
systemctl start moderation-bot.service

# Проверьте статус
systemctl status moderation-bot.service
```

**Проверка логов:**

```bash
# Логи systemd
journalctl -u moderation-bot.service -f

# Логи в файле
tail -f /root/s360-streams/logs/moderation_bot.log
```

## Рабочий процесс

1. **Новый пост в исходном канале:**
   - TelethonListener получает сообщение
   - Сохраняет в БД со статусом `'new'`

2. **Обработка GPT:**
   - GPTWorker находит пост со статусом `'new'`
   - Отправляет в GPT-4o
   - Создаёт черновик со статусом `'pending_moderation'`

3. **Модерация:**
   - ModerationBot отправляет черновик операторам
   - Оператор выбирает действие:
     - Опубликовать → выбирает каналы → выбирает картинку → публикуется
     - Править → отправляет новый текст → черновик обновляется
     - Отклонить → черновик помечается как отклонённый

4. **Публикация:**
   - Бот публикует пост во все выбранные каналы
   - Сохраняет информацию о публикации в БД
   - Статус черновика меняется на `'published'`

## Защита от дублей

- Используется комбинация `channel_id + message_id` как уникальный ключ
- При попытке добавить дубль - запись игнорируется (UNIQUE constraint)
- Редактирование исходных постов не отслеживается (только новые сообщения)

## Особенности

- **Смешанные хэштеги:** Поддерживаются русские и латинские хэштеги
- **Множественные операторы:** Все операторы видят все черновики
- **Множественные каналы:** Можно публиковать в несколько каналов одновременно
- **Обработка ошибок:** При ошибке в одном канале продолжается публикация в другие
- **Автоматическое получение картинки:** При выборе "С исходной картинкой" бот автоматически получает картинку из исходного поста
- **Детальное логирование:** Все действия логируются в файл с ротацией

## Файлы проекта

- `moderation_main.py` - главный модуль запуска
- `config_moderation.py` - конфигурация
- `database.py` - работа с БД
- `telethon_listener.py` - чтение постов из каналов
- `gpt_worker.py` - обработка через GPT
- `moderation_bot.py` - бот для операторов
- `logger_setup.py` - настройка логирования
- `moderation-bot.service` - systemd сервис
- `posts.db` - SQLite база данных
- `telethon_session.session` - сессия Telethon
- `.env` - переменные окружения (не коммитится в Git)

## Логи

Логи сохраняются в:
- `logs/moderation_bot.log` - основной лог с ротацией (10 MB, 5 файлов)
- `journalctl -u moderation-bot.service` - логи systemd

Уровни логирования:
- `DEBUG` - детальная отладочная информация
- `INFO` - общая информация о работе
- `WARNING` - предупреждения
- `ERROR` - ошибки
- `CRITICAL` - критические ошибки

